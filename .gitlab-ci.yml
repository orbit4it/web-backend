deploy:
  stage: deploy
  tags:
    - eschool
  script:
    - cd /home/ict/www/orbit-project/web-backend
    - git pull origin main
    - cat $env > .env
    - cat $log > log.ini
    - docker-compose -f ../docker-compose.yml restart backend
  only:
    - main
